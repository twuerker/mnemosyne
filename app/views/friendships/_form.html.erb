<%= form_for(@friendship) do |f| %>
  <% if @friendship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@friendship.errors.count, "error") %> prohibited this friendship from being saved:</h2>

      <ul>
      <% @friendship.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.hidden_field :user_id, :value => current_user.id %>
      Current user is <%= current_user.email %>
    </div>

    <div class="field">
      <%= f.hidden_field :friend_id, :value => params[:friend_id] %>
      Friend user is <%= User.find(params[:friend_id]).email %>
    </div>

    <div class="field">
      <%= f.hidden_field :status, :value => 'oneway' %>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
